import * as React from 'react';

import Section from '@src/components/Section';

const MainPage = () => (
  <Section title="Index">
    <div>Hello, please select something from the menu.</div>
  </Section>
);

export default MainPage;
